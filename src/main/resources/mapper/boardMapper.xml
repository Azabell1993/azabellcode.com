<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<<<<<<< HEAD
<mapper namespace="com.azabellcode.start.mapper.BoardMapper" >	<!-- xml파일을 사용할 mapper가 있는 경로 -->
	<!-- <select> 태그를 이용하여 select 쿼리임을 나타냄. 앞서 mapper에서 말했던 메소드의 이름은 select 쿼리의 id값과 같아야함. -->
		<select id="selectBoardList" resultType="com.azabellcode.start.dto.BoardDto">
			<![CDATA[
				SELECT board_idx, title, contents, creator_id, DATE_FORMAT(created_datetime, '%Y.%m.%d %H:%i:%s') AS created_datetime, hit_cnt
				FROM 
					T_BOARD TB
				WHERE
					deleted_yn = 'N'
				ORDER BY 
					board_idx DESC
			]]>
		</select>

		<insert id="insertBoard" parameterType="com.azabellcode.start.dto.BoardDto">
			<![CDATA[
				INSERT INTO t_board
				(
					title, contents, created_datetime, creator_id
				)
				VALUES
				(
					#{title},
					#{contents},
					NOW(),
					#{creatorId}
				)
			]]>
		</insert>

		<select id="boardListMap" resultType="com.azabellcode.start.dto.BoardDto">
			SELECT board_idx, title, contents, creator_id, DATE_FORMAT(created_datetime, '%Y.%m.%d %H:%i:%s') AS created_datetime, hit_cnt
			FROM 
				T_BOARD TB
			WHERE
				deleted_yn = 'N'
			ORDER BY 
				board_idx DESC
		</select>
=======
<mapper namespace="com.azabellcode.start.mapper.BoardMapper" >
    <select id ="boardMap" resultType="com.azabellcode.start.dto.BoardDTO">
		SELECT *
		FROM
		tb_board	
	</select>

	<sql id="boardColumns">
		idx
		,title
		,content
		,writer
		,view_cnt
		,notice_yn
		,secret_yn
		,delete_yn
		,insert_time
		,update_time
		,delete_time
	</sql>

	<insert id="insertBoard">
		INSERT INTO tb_board(
		<include refid="boardColumns" />
		) VALUES (
		 #{idx}
		,#{title}
		,#{content}
		,#{writer}
		,0
		,IFNULL(#{noticeYn}, 'N')
		,IFNULL(#{secretYn}, 'N')
		,'N'
		,NOW()
		,NULL
		,NULL
		)
	</insert>

	<select id="selectBoardDetail" resultType="long">
		SELECT 
			<include refid="boardColumns"/>
		FROM
			tb_board
		WHERE
			delete_yn = 'N'
		AND
			idx = #{idx}		
	</select>
	

		<update id="updateBoard" >
		UPDATE tb_board
		SET
			 update_time=NOW()
			,title = #{title}
			,content = #{content}
			,writer = #{writer}
			,notice_yn = IFNULL(#{noticeYn}, 'N')
			,secret_yn = IFNULL(#{secretYn}, 'N')
		WHERE
			idx=#{idx}	
	</update>

	<update id="deleteBoard">
		UPDATE tb_board
		SET 
			 delete_yn = 'Y'
			,delete_time = NOW()
		WHERE
			idx = #{idx}
	</update>

	<select id="selectBoardList" resultType="com.azabellcode.start.dto.BoardDTO">
		SELECT
			<include refid="boardColumns"/>
		FROM
			tb_board
		WHERE
			delete_yn ='N'
		ORDER BY
			notice_yn ASC,
			idx DESC,
			insert_time DESC
	</select>

	<select id ="selectBoardTotalCount" resultType="int">
		SELECT
			COUNT(*)
		FROM
			tb_board
		WHERE
			delete_yn='N'		
	</select>
	
	<update id="cntPlus">
		UPDATE tb_board
		SET
			view_cnt = view_cnt+1
		WHERE
			idx = #{idx}	
	</update>
	

>>>>>>> 24e21efcbb1629616b4954d556c6bb8b1fb02c75
</mapper>